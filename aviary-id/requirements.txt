# torch / torchvision are NOT listed here — they must come from the cu126 wheel index
# and are installed separately in the Dockerfile. See the note there: the cu128/cu129
# builds have no sm_61 kernels, so a plain `pip install torch` produces a container that
# silently falls back to CPU on Pascal cards (Quadro P1000, GTX 10xx).
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
open_clip_torch==3.3.0
pillow==11.1.0
numpy==2.2.1
python-multipart==0.0.20
# Supervised bird classifier runtime (TRAINED_CLASSIFIER=aiy, the default). CPU-only.
# ai-edge-litert is Google's maintained TFLite runtime; the older tflite-runtime
# package (last built 2023, against numpy 1.x) crashes on import next to numpy 2.
ai-edge-litert==2.2.0
# YOLO11n bird localizer (DETECTOR_BACKEND=yolo, the default). AGPL-3.0 — see the README
# licensing note. Its torch/torchvision requirements are already satisfied by the cu126
# wheels installed in the Dockerfile, so pip will not touch them. Must be 8.4+: the 8.3
# line capped numpy at <=2.1.1, which conflicts with the numpy pinned above; 8.4 dropped
# the upper bound entirely.
ultralytics==8.4.120
